[project]
name = "lazygit"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
lazygit = "lazygit.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
