[project]
name = "lazygit"
version = "0.1.0"
description = "A small Git helper REPL"
requires-python = ">=3.10"
dependencies = [
    "prompt-toolkit>=3.0"
]

[project.scripts]
lazygit = "lazygit:repl_loop"
